{
  "name": "dreamfeed-command-center",
  "version": "0.1.0",
  "private": true,
  "description": "Dreamfeed — read-only, localhost-only governance cockpit. Zero runtime dependencies.",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "test:integration": "node test/run-integration.js"
  }
}
