# Moved

The `dreamfeed-design` skill definition now lives at
`.claude/skills/dreamfeed-design/SKILL.md`, where Claude Code discovers it.

The design-system content itself (tokens, assets, components, guidelines,
UI kits) remains in this directory — start with `README.md`.
