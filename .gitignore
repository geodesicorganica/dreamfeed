# repo-harness-auditor sidecar — generated by `node audit.js`, read by Repo Health.
# Transient status record; not committed (the cockpit reads it; it is not source).
audit-status.json
# Active-project sidecar — written by the server when the operator switches the
# project root. Holds absolute LOCAL paths; machine-specific, never committed.
project-config.json
node_modules/
